language: python
os: linux
python:
- 3.4
- 2.7

env:
  - SIT
  - UAT

script: python3 test_fruits.py

# list of branches
branches:
  only:
    - master
    - /[upt][a-q]*[t]\b//
  except:
    - opt

jobs:
  exclude:
    - python: 3.4
      env: SIT

# comments
